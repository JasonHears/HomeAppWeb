export default class View {
  _data;

  render(data) {
    console.log("View.render()", data);
    // handle displaying error if no data
    // generate markup
    // clear existing content
    // insert new content
  }
}
